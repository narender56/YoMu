import { createStore } from 'vuex'

import profile from './profile'

const state = {}

const modules = {
  profile
}

const getters = {}

const mutations = {}

const actions = {}

export default createStore({
  state,
  getters,
  mutations,
  actions,
  modules
})
